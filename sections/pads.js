const programChangePadsContainer = document.querySelector(
  '.prog-change-container'
);
const padsButtons = document.querySelectorAll(
  '[data-id="patch-number-btn-pad"]'
);

let section = 'pads' | 'userscale';
